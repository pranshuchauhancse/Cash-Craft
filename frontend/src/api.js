import axios from 'axios'; export const API = axios.create({ baseURL: (import.meta.env.VITE_API_BASE || 'http://localhost:5000') + '/api' }); API.interceptors.request.use(cfg=>{ const t = localStorage.getItem('token'); if(t) cfg.headers.Authorization = 'Bearer ' + t; return cfg; });
